name: licomesh-audit-gate

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  licomesh-audit-gate:
    name: licomesh-audit-gate
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout target repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          path: target

      - name: Checkout audit source of truth
        uses: actions/checkout@v5
        with:
          repository: LicoLand/licomesh-audit
          ref: only
          fetch-depth: 0
          path: licomesh-audit

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Verify audit source of truth
        run: |
          licomesh-audit/bin/licomesh-audit source-of-truth \
            --repo licomesh-audit \
            --remote origin \
            --branch only \
            --require-current-head \
            --enforce-remote-heads

      - name: Run LicoMesh audit gate
        run: |
          licomesh-audit/bin/licomesh-audit gate \
            --repo target \
            --history \
            --format text
